```{bash}
$ python3.6 -m venv deep-venv
$ source deep-venv/bin/activate
(deep-venv) $ pip install --upgrade pip
(deep-venv) $ pip install pillow numpy matplotlib h5py tensorflow keras # run tensorflow as cpu
(deep-venv) $ pip install pillow numpy matplotlib h5py tensorflow-gpu keras # run tensorflow as gpu
(deep-venv) $ sudo apt-get install python3.6-tk
```
